## Версия 1.9.1 (2025-11-29)

*Унифицировал заголовки разделов с эмодзи в changelog.*

### 🐛 Исправления ошибок

- Все разделы Release Notes теперь используют одинаковые эмодзи в заголовках, без артефактов `??`, чтобы список версий читался консистентно.

## Версия 1.9.0 (2025-11-29)

*Голосовой запуск приложений с запросом прав администратора.*

### ✨ Новые возможности

- Голосовые команды теперь умеют просить запуск «от имени администратора»: при таких запросах команда оборачивается в `Start-Process ... -Verb RunAs`, что вызывает UAC и стартует приложение с повышенными правами.

### 🐛 Исправления ошибок

- В логах отображается, запрошен ли админ-режим, чтобы проще отлавливать сценарии, когда окно UAC не появилось.

## Версия 1.8.6 (2025-11-29)

*Возвращён прокси в клиент Gemini, чтобы V2Ray/VLESS снова проходили без 403.*

### 🐛 Исправления ошибок

- HTTP-клиент Gemini снова читает `HTTPS_PROXY` (V2RayN/VLESS), убран кастомный транспорт, который обходил прокси и приводил к 403 Forbidden даже с валидным ключом.
- В логах теперь видно, какой прокси используется при инициализации Gemini, чтобы быстрее диагностировать сетевые сбои.

## Версия 1.8.5 (2025-11-27)

*Поиск Everything не падает на 403 и автоматом пробует запасные модели Gemini.*

### 🐛 Исправления ошибок

- Нормализация голосового поиска через Gemini теперь ловит 403 Forbidden и сетевые ошибки без длинных HTML в логах, возвращая понятное сообщение.

### ⚡ Улучшения

- Добавлен фолбэк на другие модели Gemini (Flash → Pro → 3 Pro) при ошибках нормализации поиска, чтобы команды «найди …» чаще отрабатывали с первого раза.

## Версия 1.8.4 (2025-11-24)

*Устойчивое подключение к Gemini даже через нестабильные прокси.*

### 🐛 Исправления ошибок

- Gemini HTTP-клиент теперь принудительно использует HTTP/1.1, тайм-ауты и встроенные ретраи httpx, что снижает вероятность `SSL: UNEXPECTED_EOF_WHILE_READING` при работе через SOCKS5/VPN.

### ⚡ Улучшения

- Добавлены короткие повторные попытки и распознавание сетевых/SSL-сбоев перед переключением на резервную модель, чтобы ответы Gemini чаще приходили без ручного повтора.

## Версия 1.8.3 (2025-11-23)

*Надёжнее удержание хоткеев и поиск папок без лишнего шума.*

### 🐛 Исправления ошибок

- Удержание Win+Shift / Ctrl+Shift срабатывает даже если pynput добавляет управляющие коды в множество нажатых клавиш; Alt+Space удалён, чтобы не ловить ложные комбинации.
- Голосовой поиск папок через Everything больше не переключается на поиск файлов при слове «видео», а в логе остаются только ключевые строки запуска и выбранного результата.

## Версия 1.8.2 (2025-11-23)

*Хоткеи без ложных срабатываний.*

### 🐛 Исправления ошибок

- Фильтрация управляющих символов в хоткеях (например, `\x11` от Ctrl) убирает ложные нажатия и повторные срабатывания, удержание теперь работает стабильно.

## Версия 1.8.1 (2025-11-23)

*Мелкие правки для стабильности и сборки.*

### 🐛 Исправления ошибок

- Базовые слова активации моделей по умолчанию возвращены к «про» и «флеш», чтобы переключение работало даже без сохранённых настроек.

### ⚡ Улучшения

- Скрипт post_build копирует `settings.json` в итоговую сборку, чтобы пользовательские настройки попадали рядом с exe.

## Версия 1.8.0 (2025-11-23)

*Прилепание окна и удобная очистка логов.*

### ✨ Новые возможности

- В окне логов появилась кнопка «Очистить» для мгновенного сброса текущего файла журнала.

### 🐛 Исправления ошибок

- Проверка VLESS учитывает состояние переключателя: тест не показывает «активен», если VPN выключен, а при отключении соединение закрывается.
- Хоткеи по удержанию обрабатываются надёжнее благодаря нормализации Shift и расширенному логированию нажатий.

### ⚡ Улучшения

- Frameless-окно ассистента прилипает к краям экрана через кастомный snap-помощник, как стандартные окна.

## Версия 1.7.3 (2025-11-23)

*Точное удержание хоткея и автосворачивание при тишине.*

### 🐛 Исправления ошибок

- Запись по удержанию теперь стартует только при точном совпадении выбранной пары клавиш; любое третье нажатие прекращает диктовку.
- После обнаружения тишины или пустой записи мини-окно автоматически сворачивается в трей, статус быстрее возвращается в режим ожидания.

## Версия 1.7.2 (2025-11-23)

*Чистка артефактов компилятора.*

### 🐛 Исправления ошибок

- Настройки компилятора (`PyCompiler_settings.json`) и кеш `__pycache__` в Build_Tools больше не попадают в репозиторий благодаря обновлённому `.gitignore`.

## Версия 1.7.1 (2025-11-23)

*Гибкое окно компилятора и запоминание размещения.*

### ⚡ Улучшения

- GUI-компилятор SpecCompiler сохраняет размер, позицию и состояние развёрнутости окна между запусками.
- Сняты жёсткие ограничения на ширину/высоту: оставлен только минимальный размер, окно можно свободно растягивать в обе стороны.

## Версия 1.7.0 (2025-11-23)

*Быстрый доступ к логам и безопасный поиск без пустых совпадений.*

### ✨ Новые возможности

- Автооткрытие окна логов, когда запуск программ, поиск Everything или открытие ссылок завершаются неудачно — окно сразу раскрывается и прокручивается к концу.
- В контекстном меню иконки в трее добавлен пункт «Открыть логи» для мгновенного просмотра журнала.

### 🐛 Исправления ошибок

- Поиск через Everything больше не открывает корень диска, если имя потерялось; при аудио-фильтрах имя запроса сохраняется, а при отсутствии совпадений выводится корректный статус без открытия лишних путей.

## Версия 1.6.3 (2025-11-23)

*Настройка удерживаемых хоткеев и стабильность запуска записи.*

### 🐛 Исправления ошибок

- Устранён `IndentationError` в обработчиках выбора горячих клавиш; переключение режимов снова работает.
- Нормализованы клавиши (левый/правый Ctrl/Alt/Cmd) для корректной проверки новых сочетаний удержания.
- В настройках переименован блок в «Режим удержания» и сохранение выбранного сочетания (Win+Shift, Ctrl+Shift, Alt+Space) теперь применяется сразу.

## Версия 1.6.2 (2025-11-23)

*Сессионные логи и сборка с новыми модулями поиска.*

### 🐛 Исправления ошибок

- Лог-файл очищается в silent-режиме при выходе, без добавления служебных строк — лог доступен в течение сессии и не накапливается между перезапусками.
- В сборку PyInstaller добавлен модуль `everything_file_filters.py`, чтобы голосовой поиск по типам файлов работал в exe без ручного добавления.
- `.gitignore` разрешает отслеживать модули поиска (`everything_search.py`, `everything_search_prompt.md`, `everything_file_filters.py`), чтобы они всегда попадали в репозиторий и сборку.

## Версия 1.6.1 (2025-11-23)

*Устойчивость поиска и управление логами по сессиям.*

### 🐛 Исправления ошибок

- Категории файлов (музыка/видео/документы и т.д.) определяются по всей фразе и по подстрокам (стемы «музык», «докум», «архив»), чтобы запросы вроде «найди музыку …» не воспринимались как поиск папки.
- Очищение логов вынесено в `silent`-режим при выходе: логи доступны в течение сессии, но файл очищается при закрытии приложения без служебных строк.

## Версия 1.6.0 (2025-11-22)

*Голосовой поиск файлов по категориям через Everything.*

### ✨ Новые возможности

- Голосовой поиск файлов с категоризацией: «Найди музыку/видео/документы/архивы/фото/программы на диске …» включает фильтр расширений `ext:` и фаззи-паттерн по имени, как у папок.
- Поддержка сочетания категории и имени: можно сказать «найди видео … День сурка» или «найди музыку … Переведенные комментарии», фильтр по типу и поиск по имени выполняются одновременно.

### 🐛 Исправления ошибок

- Определение категории идёт по всей фразе, даже если Gemini вернул `folder`, поэтому запросы «найди музыку …» больше не ищут папку.
- Regex-поиск теперь тоже включает `ext:` фильтр, чтобы категория применялась и в фаззи-режиме.
- Расширен словарь ключевых слов категорий (стемы «музык», «докум», «архив» и т.д.) для более надёжного распознавания.
- .gitignore дополнен новыми модулями поиска и фильтров, чтобы они всегда попадали в сборку.

## Версия 1.5.2 (2025-11-22)

*Устойчивость голосового поиска и запусков Gemini-команд.*

### 🐛 Исправления ошибок

- Окно ассистента теперь гарантированно сворачивается в трей после команд запуска и поиска, даже при ошибке или отклонении.
- Приём команд от Gemini не обрезается по длине: корректно выполняются строки вида `Start-Process ... -Verb RunAs`.
- Голосовой поиск через Everything: учитываются системные алиасы (например, «Пользователи» → `C:\Users`), знаки препинания в названии игнорируются при формировании шаблона, прямые пути на выбранном диске проверяются приоритетно.

## Версия 1.5.1 (2025-11-22)

*Устранение проблем запуска и скрытия окна после команд.*

### 🐛 Исправления ошибок

- Команды от Gemini принимаются целиком (например, запуск cmd с повышением прав), больше не отклоняются из-за длины строки.
- Окно ассистента сворачивается в трей после выполнения или ошибки команд запуска программ и поиска через Everything, чтобы не оставаться на экране.

## Версия 1.5.0 (2025-11-22)

*Голосовой поиск файлов и папок через Everything с нормализацией Gemini.*

### ✨ Новые возможности

- **Поиск через Everything ES CLI голосом**: команды «Найди папку/файл … на диске …» разбираются Gemini (исправление опечаток, понимание описаний формата имени, языка, разделителей) и превращаются в запрос к `es.exe` с комбинированным regex+прямым поиском.
- **Гибкие шаблоны имен**: поддерживается разбор инструкций вида «два ноля, нижнее подчеркивание, слово развитие», «английские слова через подчеркивание», «два слова на английском» и т.п. для точного формирования имени.

### 🐛 Исправления ошибок

- Строки результатов `es.exe` читаются через экспорт в UTF-8 BOM, что устраняет кракозябры и позволяет открывать пути с кириллицей.
- Улучшено ранжирование результатов Everything: учитывается сходство имени и минимальная глубина, чтобы выбирать корневые папки, а не вложенные каталоги.
- Регулярные выражения для многословных запросов строятся с учетом всех слов и корректных разделителей `[\s\S]*?`, без лишнего префикса `.+`.
- В сборку добавлены новые файлы (`everything_search.py`, `everything_search_prompt.md`), обновлён allow-лист `.gitignore`.

## Версия 1.4.0 (2025-11-21)

*Голосовое управление программами и апплетами Windows.*

### ✨ Новые возможности

- **Запуск программ голосом**: команда «Запусти [название]» открывает программы, апплеты Windows и системные утилиты.
- **Умное распознавание команд**: встроенный словарь популярных программ (калькулятор, блокнот, панель управления, браузеры и др.) обеспечивает мгновенный запуск.
- **Gemini AI для неизвестных программ**: если команда не найдена в словаре, Gemini определяет нужную программу по описанию (например, «программа для рисования» → Paint).
- **Защита от опасных команд**: автоматическое обнаружение потенциально опасных операций (удаление файлов, форматирование, изменение реестра) с обязательным диалогом подтверждения.
- **Полное логирование**: все выполненные команды записываются в лог с отметками о подтверждении или отклонении.

### 🛠 Исправления

- Упрощена система триггеров голосовых команд: теперь только формы слова «открой» используются для сайтов, а формы слова «запусти» — для программ. Это устраняет конфликты при распознавании команд.
- Исправлена проблема, из-за которой окно не сворачивалось обратно в компактный режим после выполнения команд запуска программ и открытия сайтов. Теперь окно автоматически минимизируется, как и после диктовки.

### ⚡ Улучшения

- Добавлен словарь из 40+ популярных Windows команд и программ для мгновенного запуска без обращения к Gemini.
- Реализован механизм обнаружения опасных команд с использованием регулярных выражений для паттернов (del, format, reg delete и т.д.).

## Версия 1.3.1 (2025-11-21)

*Локализация поисковой системы для российских пользователей.*

### 🛠 Исправления

- Поисковая система для голосовых команд изменена с Google на Яндекс для улучшения доступности в России.

## Версия 1.3.0 (2025-11-21)

*Голосовая навигация по сайтам и умный поиск URL.*

### ✨ Новые возможности

- **Открытие сайтов голосом**: команда «Открой [название]» запускает браузер.
- **Smart URL Resolution**: если сайт неизвестен, Gemini находит его официальный адрес (например, «сайт налоговой» -> `nalog.gov.ru`).
- **Мгновенный доступ**: популярные ресурсы (YouTube, VK, Яндекс Почта и др.) открываются мгновенно через встроенный словарь.

### 🛠 Исправления

- Улучшено распознавание команд: фразы теперь корректно обрабатываются, даже если Whisper разбивает их на части (split segments).

## Версия 1.2.1 (2025-11-20)

*Поддержка файла VERSION в репозитории и порядок changelog.*

### 🛠 Исправления

- Файл VERSION добавлен в whitelist `.gitignore`, чтобы версия всегда попадала в репозиторий и сборки.
- Последние версии выводятся первыми: порядок записей в changelog упорядочен.

## Версия 1.2.0 (2025-11-20)

*Голосовое переключение профилей и моделей, устойчивое к паузам и тире.*

### ✨ Новые возможности

- Первое слово диктовки выбирает профиль промпта (например, «Диктовка» или «Ассистент»), а следующее слово «про/флеш» — модель; распознавание спокойно переживает тире и паузы.
- Голосом выбранный профиль сразу применяется в настройках и используется как активный текст промпта.

### 🛠 Исправления

- Комбинации вроде «ассистент – флеш» теперь корректно переключают Flash даже при дефисах и пунктуации.
- Режим «Выделить» устойчиво обрабатывает активационные слова и игнорирует пустые токены.

## Версия 1.1.1 (2025-11-20)

*Исправление упаковки версии.*

### 🛠 Исправления

- Файл VERSION теперь копируется в собранную папку, чтобы приложение всегда показывало актуальный номер версии после сборки.

### ⏱ Надёжность

- Исключён показ `vdev` в заголовке при отсутствии VERSION в каталоге сборки.

## Версия 1.1.0 (2025-11-20)

*Thinking-настройки и безопасная отмена с Whisper-фолбэком.*

### ✨ Новые возможности

- Добавлены переключатели thinking в настройках: Gemini 3 (High/Low) и Gemini 2.5 Flash (Thinking/Быстрый) с сохранением в конфиг.
- Унифицированные подписи моделей и отображение версии из файла VERSION в окне и системном трее.

### 🛠 Исправления

- thinking_level теперь отправляется только там, где его поддерживает SDK; для 2.5 моделей используется thinking_budget (Pro всегда -1, Flash 0/-1).
- Экстренная отмена по ПКМ на стрелке разворачивания сразу вставляет текст Whisper и сбрасывает зависшие запросы Gemini.

### ⏱ Надёжность

- Фолбэки между моделями пересчитывают режим thinking под целевую модель и корректно логируются, прокси/инициализация Gemini отражаются в логах.

## Версия 1.0.4

*Поддержка Gemini 3.0 и цепочка откатов.*

### ✨ Новые возможности

- Добавлена поддержка Gemini 3.0 Pro; голосовая команда «про» использует Gemini 3.0 Pro Preview как основной вариант.

### ⏱ Надёжность

- Цепочка fallback: Gemini 3.0 Pro Preview → Gemini 2.5 Pro → Gemini 2.5 Flash.

### 🛠 Прочее

- В состав поставки добавлен `nircmd.exe`, чтобы исключить проблемы отсутствия утилиты.
- Обновлён README с описанием новых режимов и команд.

## Версия 1.0.3

*Упаковка вспомогательных утилит.*

### 🛠 Исправления

- `nircmd.exe` включён в поставку по умолчанию.

## Версия 1.0.2

*Визуальные доработки.*

### 🛠 Прочее

- Новая иконка приложения с 3D-логотипом в системном трее.

## Версия 1.0.1

*Включение Gemini 3.0 Pro Preview по умолчанию.*

### ✨ Новые возможности

- Gemini 3.0 Pro Preview используется как базовая модель.
